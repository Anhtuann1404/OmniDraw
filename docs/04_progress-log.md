# OmniDraw — Progress & Decisions Log

**Mục đích:** Ghi lại các mốc quan trọng và quyết định đã chốt — không log chi tiết từng ngày, chỉ ghi những gì đáng nhớ (quyết định kỹ thuật, thay đổi hướng đi, hoàn thành mốc lớn). File này sẽ tiết kiệm rất nhiều thời gian khi viết báo cáo cuối kỳ.

> Ghi mới nhất lên đầu (thêm dòng phía dưới tiêu đề, không xoá log cũ).

---

## Log


| Ngày     | Loại           | Nội dung                                                                                                                                      |
| -------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| (23/9)   | Hoàn thành mốc (TV1) | **Triển khai và kiểm chứng toàn diện Scan-Validation Pipeline cho phiếu thu thập P01–P04 (TV1):** Lập trình hoàn chỉnh package `backend/scan_validator/` (`specs.py`, `fiducial_detector.py`, `rectifier.py`, `calibration_checker.py`, `qc_evaluator.py`, `auto_cropper.py`, `pipeline.py`) và CLI runner xử lý ảnh quét phẳng 600 DPI: (1) Tự động phát hiện 4 mốc quang học fiducial 5x5mm tại 4 góc với lọc sub-pixel và xác thực khoảng cách tâm hình học; (2) Tự động nắn chỉnh phối cảnh / deskew đưa về hệ tọa độ chuẩn ISO A4 (4961x7016 px tại 600 DPI); (3) Kiểm chuẩn đo lường thước đo 50mm (dung sai ±0.2mm) và ô vuông tỷ lệ cạnh 20x20mm (Aspect Ratio 1.000 ± 0.005); (4) Tự động cắt tách ô mẫu chữ viết tay tất định cho toàn bộ biểu mẫu P01 (24 ô), P02 (16 từ), P03 (3 câu) và P04 (1 đoạn); (5) Đánh giá kiểm định chất lượng quang học (QC tràn viền lề và nét dơ); (6) Tạo bộ test tự động toàn diện `tests/test_scan_validation_pipeline.py` với 11/11 tests PASS; toàn bộ test suite repository đạt **126/126 passed** (100% PASS). |
| (23/9)   | Hoàn thành mốc (TV1) | **Hoàn tất Cross-review Báo cáo NCKH Chương 1–3 và Đóng băng Benchmark Corpus CA-VHC v1.0 (TV1):** (1) Hoàn tất toàn diện phiếu Cross-review TV1 tại [`docs/reviews/tv1_chapter_1_3_review.md`](reviews/tv1_chapter_1_3_review.md) (`WORKFLOW_STATUS: CLOSED`, `AI_DRAFT_VERDICT: PASS`, `HUMAN_VERDICT: PASS`), đóng 3 finding `TV1-R01` (ranh giới dữ liệu thu thập vs benchmark kỹ thuật), `TV1-R02` (kỷ luật phân tách DEV/Holdout chống rò rỉ dữ liệu) và `TV1-R03` (phân ranh học thuật giữa CA-VHC P0 và Writer Profile P2); (2) Ban hành Báo cáo phân tích độ phủ ngôn ngữ học/hình học và chính thức phê duyệt đóng băng Benchmark Corpus phiên bản `CA-VHC-CORPUS-v1.0-FROZEN` tại [`docs/19_benchmark_corpus_freeze_report.md`](19_benchmark_corpus_freeze_report.md) gồm 20 từ DEV, 20 từ Holdout (kiểm chứng độc lập tuyệt đối $DEV \cap HOLDOUT = \emptyset$) và 10 mẫu nghiệm thu PR3 trích xuất từ DEV; (3) Bổ sung `CORPUS_VERSION` vào `backend/handwriting/benchmark_fixtures.py`; (4) Kiểm chứng tự động toàn bộ test suite đạt **115/115 passed** (100% PASS). |
| (23/9)   | Hoàn thành mốc (TV3) | Hoàn tất bản nháp Cross-review Báo cáo NCKH Chương 1–3 (Phiếu TV3 tại `docs/reviews/tv3_chapter_1_3_review.md`): Rà soát độc lập các mục `10_nckh_research_plan.md` (RQ3, clearance, physical feasibility), Chương 1 (1.4.3, 1.8–1.9), Chương 2 (2.5–2.6) và Chương 3 (3.1, 3.7–3.8). Xác nhận: (1) Wording tài liệu tuân thủ nghiêm ngặt ranh giới giữa simulator time và `actual_draw_time_sec`; (2) Clearance hình học $0.20\,\text{mm}$ (technical minimum) và $0.50\,\text{mm}$ (provisional target) không bị biến thành claim an toàn cơ khí; (3) Toàn bộ physical validation được gắn nhãn chuẩn `PENDING_TV3_CALIBRATION`; AI draft verdict đạt `PASS`. Đồng thời kiểm chứng test suite trên nhánh `develop` đạt 115/115 passed. |
| (22/9)   | Hoàn thành mốc (TV3) | Hoàn thiện tích hợp adapter và dọn sạch runtime artifacts: (1) Đồng bộ thành công `origin/develop` vào `feature/hardware`, xử lý triệt để xung đột nội dung và xóa 100% git conflict markers; (2) Dọn sạch `backend/main.py`: loại bỏ hoàn toàn các biến/hàm mock (`jobs`, `DEVICE_CONNECTED`, `_run_job`, `_parse_path_length_mm` trùng lặp, `svg_estimate_draw_time` trùng lặp), nối lại luồng chuẩn cho `generate_ai_image`, chuẩn hóa 1 bộ print routes duy nhất gọi `hardware_adapter` với `StartPrintRequest`, `cancel_print` bắt buộc `await hardware.cancel_job(body.request_id)`; (3) Chuyển nguồn `csv_logger.py` sang `backend/csv_logger.py` (được Git theo dõi), sửa import trong `backend/main.py` và `backend/api_generator.py` thành `from csv_logger import log_experiment_csv`, đồng thời re-export tại `backend/logs/csv_logger.py` để bảo toàn tương thích ngược; (4) Untrack (`git rm --cached`) toàn bộ runtime artifacts (`backend/tmp_print/*_prepared.svg`, `logs/hardware_metrics.csv`, `logs/test_report_*.txt`), bổ sung vào `.gitignore` và tạo `logs/.gitkeep`; (5) Kiểm chứng tự động 100% PASS: `py_compile` backend/main.py, hardware_adapter.py, csv_logger.py không lỗi cú pháp; `import main` từ thư mục `backend/` thành công; toàn bộ test suite đạt 90/90 passed (trong đó test phần cứng `tests/test_hardware_adapter.py` đạt chuẩn xác 23/23 passed). |
| (22/9)   | Hoàn thành mốc | **TV2 — PR2 Baseline Adapters B1/B2/B3:** Thêm `backend/handwriting/baselines.py` với ba method tag độc lập: B1 `b1_static` (canonical glyph, luôn lift), B2 `b2_greedy` (quyết định cục bộ theo trạng thái đã chọn trước đó), B3 `b3_current_trellis` (Viterbi hiện hành). Mở rộng internal structured-render/experiment runner bằng `_algorithm_mode` và CLI `--method`, không đổi public API. Thêm `tests/test_ca_vhc_baselines.py`; kết quả kiểm chứng: **73 passed**, Handwriting Self-Check PASS, Path Optimizer Self-Check PASS, `git diff --check` sạch. Entry Gate E3 hoàn tất; E4 shared transition interface vẫn chờ TV2+TV4 ký duyệt. |
| (22/9)   | Hoàn thành mốc | TV4 khóa quy tắc nghiệm thu định lượng bridge–diacritic trước PR3: tập trung hai ranh giới phán quyết phần mềm $0.20\,mm$ và mục tiêu tạm thời $0.50\,mm$ trong `metrics_evaluator.py`; bổ sung hàm phân loại fail-closed với bốn verdict máy đọc được; chuyển 10 mẫu nghiệm thu tiếng Việt thành nguồn dữ liệu DEV-only dùng chung trong `benchmark_fixtures.py`; thêm unit test điểm biên và input metric không hợp lệ. Đây chỉ là xác nhận logic phần mềm; hiệu chuẩn an toàn vật lý của TV3 vẫn `PENDING`. |
| (21/9)   | Quyết định     | **CA-VHC-DEC-001 — Step B Closure and Step C Software Authorization**<br>• **Owner quyết định:** TV4 (Project Lead & Handwriting / CA-VHC Composition Lead); **Technical Reviewer:** TV2 (Stroke Optimization Lead) — verdict: **PASS**.<br>• **Trạng thái:** **Step B: APPROVED AND CLOSED**; **Step C: AUTHORIZED / NOT YET IMPLEMENTED**; **READY FOR STEP C: YES** (chính thức cho phép triển khai mã nguồn phần mềm Step C, không đồng nghĩa Step C đã hoàn thành hay thuật toán đã được chứng minh).<br>• **Khóa 5 quyết định kỹ thuật phần mềm:**<br>  1. *Clearance:* $0.20\text{ mm}$ là giá trị mặc định kỹ thuật ban đầu để bảo toàn tương thích code hiện tại; $0.50\text{ mm}$ là research target cần TV3 hiệu chuẩn trên máy vẽ thật; quản lý qua `DiacriticConfig` khi Step C triển khai, không hard-code rải rác; không tuyên bố $0.50\text{ mm}$ đã được xác nhận thực nghiệm.<br>  2. *Metric/API Contract:* Internal evaluator (`metrics_evaluator.py`) và structured trace đã tồn tại; metric CA-VHC mới chỉ thêm vào public API/CSV sau khi backend thực sự xuất dữ liệu ổn định; API Spec cập nhật bằng PR riêng sau integration; `optimize_time_ms` trong API Spec là documentation debt đã biết.<br>  3. *Delayed-Stroke P0 Policy:* Viết xong thân từ $\rightarrow$ thu thập nét phụ/dấu trễ $\rightarrow$ sắp xếp bằng deterministic Nearest Neighbor từ điểm kết thúc thân từ $\rightarrow$ dùng index gốc làm tie-break; Exact Enumeration ($M \le 6$) thuộc P1; Writer Profile thuộc P2 (đây là quyết định thiết kế, chưa phải code đã triển khai).<br>  4. *Benchmark Corpus:* `BENCHMARK_DEV_CORPUS_20` và `BENCHMARK_HOLDOUT_CORPUS_20` tồn tại ở mức **PROVISIONAL TECHNICAL FIXTURE**; TV1 vẫn phải review coverage và đóng băng version trước thực nghiệm chính thức (không chặn viết code Step C, nhưng chặn tuyên bố kết quả thực nghiệm).<br>  5. *Quyền bắt đầu Step C:* TV2 technical review PASS; TV4 cho phép bắt đầu software implementation; TV1 corpus freeze và TV3 hardware calibration là downstream validation gates.<br>• **Downstream Validation Gates còn mở:** TV1 corpus freeze: **PENDING**; TV3 physical clearance calibration: **PENDING**; Formal experiment: **NOT STARTED** (**FORMAL EXPERIMENT READINESS: NO**).<br>• **Nhiệm vụ đầu tiên:** Khởi động `PR1 — Hoàn thiện CA-VHC Metrics & Experiment Infrastructure`. Quyết định này tuyệt đối không được coi là bằng chứng về hiệu quả thuật toán. |
| (20/9)   | Hoàn thành mốc | TV1: Hoàn tất thiết kế và technical specification bộ phiếu thu thập mẫu chữ viết tay tiếng Việt **OmniDraw Handwriting Collection Sheet Pack v1** (P01: Isolated Characters & Diacritics, P02: Context & Ligatures, P03: Sentence Flow & Pangrams, P04: Natural Paragraph). Cả 4 trang đã qua nhiều vòng rà soát chặt chẽ: geometry A4, 4 fiducial markers, calibration ruler & square, deterministic bounding boxes (`bbox_mm`, `writing_bbox_mm`), No-PII boundary, và làm sạch văn phong học thuật (loại bỏ suy diễn độ mỏi, phân định rạch ròi dữ liệu quét tĩnh vs. động học). Trạng thái hiện hành: **PILOT CANDIDATE — AWAITING PILOT PRINT & SCAN VALIDATION (NOT APPROVED FOR FORMAL DATA COLLECTION)**. Toàn bộ lưu trữ tại `docs/collection_sheets/P01..P04`. Chưa triển khai quét thực tế và chưa chạy pilot. |
| (20/9)   | Hoàn thành mốc & Quyết định | TV4: (1) Hoàn tất kiểm chứng Strict Validation toàn diện (34/34 automated regression tests passed trong repository đầy đủ, bảo đảm validation-before-side-effect và mode-aware contract). (2) Hoàn tất BƯỚC A — Architecture Audit Trellis DAG / CA-VHC hiện tại (`docs/06_audit_trellis_dag_report.md`), bóc tách chi tiết: Trellis DAG hiện chỉ tối ưu biến thể thân chữ cơ sở (`GlyphVariant`), dấu tiếng Việt hoàn toàn xử lý ở pha hậu kỳ (`generate_accents()` post-DAG), kiểm tra va chạm cầu nối runtime chưa xét dấu, recurrence Viterbi $O(L \cdot K^2)$ với $K \le 3$. (3) Hoàn thành bản dự thảo thiết kế kiến trúc BƯỚC B — Diacritic-Aware State Representation (`docs/07_diacritic_aware_state_design.md`) theo mô hình Hybrid (`GlyphVariant` + `DiacriticCandidate` $\rightarrow$ `CompositionState`), thực hiện đợt rà soát kỹ thuật (technical cleanup): phân tách rõ hệ tọa độ local/world, loại bỏ double-count chi phí vị trí, xác lập Single Source of Truth `DiacriticConfig`, khống chế candidate thô $K_{\text{raw}} \le 9$ chỉ cắt tỉa hard-invalid, và xây dựng danh mục 12 unit tests; trạng thái: hoàn thành bản dự thảo, chờ phiên review chéo (cross-review) từ TV2 trước khi bước vào triển khai mã nguồn Bước C (chưa sửa source code, chưa triển khai delayed-stroke hay thay đổi API contract). |
| (19/9)   | Quyết định     | Chuẩn hóa và phân tách rạch ròi hai nhánh dataset chữ viết tay: (1) **Vietnamese Diacritic / CA-VHC Dataset** tập trung vào cấu trúc, chính tả, anchor mỏ neo, diacritic offset và allographs ngữ cảnh (TV4 sở hữu quy tắc & logic, TV1 chuẩn bị data); (2) **Writer Profile / Personalization Dataset** tập trung vào đặc trưng cá nhân hóa phong cách người viết (TV1 sở hữu, nghiên cứu mở rộng P2). Xác lập nguyên tắc Shared Raw Input cho phép dùng chung bản quét gốc nhưng phân tách độc lập hoàn toàn về pipeline, schema và thư mục lưu trữ. Ban hành tài liệu đặc tả kỹ thuật `docs/08_handwriting_dataset_spec.md` gồm 12 chương quy định chi tiết protocol thu thập, độ phủ prompt set, raw data spec, directory layout, schema JSON, writer-disjoint split chống rò rỉ dữ liệu, đạo đức/anonymization và implementation status matrix. |
| (18/9)   | Hoàn thành mốc | Hoàn tất Strict Validation & Final Validation Hardening cho cả Handwriting Mode và Art Mode (TV4): loại bỏ hoàn toàn silent fallback cho `style`, `font`, `letter_type`, `seed`, `target_paper_size_mm` (trả các mã lỗi có cấu trúc `INPUT_INVALID_FORMAT`, `UNSUPPORTED_FONT`, `UNSUPPORTED_LETTER_TYPE`, `INVALID_SEED`, `EMPTY_TEXT`). Chuẩn hóa contract Art Mode: khuyết/null `style` resolve an toàn thành `effective_style = "sketch"`; `style` sai/ngoài enum hoặc payload rỗng bị từ chối ngay trước pipeline với `INPUT_INVALID_FORMAT`; kiểm chứng downstream (`call_openai_image_api` và `svg_process`) luôn nhận `effective_style`, không bao giờ nhận `None`. Validation diễn ra nghiêm ngặt trước mọi side effect: request invalid tuyệt đối không gọi `_clear_cached_svg_for_request` hay làm thay đổi cached SVG/artifact hiện có; request hợp lệ thực hiện dọn dẹp đúng 1 lần trước khi pipeline chạy. Toàn bộ 34 automated tests trong `backend/test_handwriting_validation.py` đạt 34/34 passed trong repository đầy đủ. |

| (21/9)   | Hoàn thành mốc (TV3) | Hoàn thiện toàn diện module Hardware Abstraction Layer TV3 v1.4 đạt 100% PASS (23/23 tests): (1) Phân định rạch ròi chế độ máy thật vs fake: khi yêu cầu `physical` mà thiếu `pyaxidraw`, hệ thống báo lỗi không kết nối (`HARDWARE_NOT_CONNECTED`), tuyệt đối KHÔNG tự ý tráo sang fake driver; fake driver chỉ bật khi được chỉ định tường minh (`use_fake_driver=True` hoặc `mode='fake'`); (2) Điều khiển Pause/Cancel thực sự trên fake driver & worker bằng `threading.Event`, loại bỏ triệt để lỗi worker tự ý ghi 'done' khi đang pause; (3) Nâng cấp parser SVG bằng `xml.etree.ElementTree`: duyệt phân cấp cây DOM tính đúng hệ số transform scale lồng nhau cho các nhóm `<g transform="scale(2)">` (đường 10mm tính đúng 20s ở v=1mm/s); (4) Quy đổi chính xác offset an toàn 5mm sang đơn vị viewBox tương ứng trước khi chèn vào SVG; (5) Xác thực XML nghiêm ngặt: từ chối chuỗi malformed XML như `<svg><path></svg>` và chuỗi phi SVG trả lỗi `SVG_INVALID`; (6) Bổ sung lệnh `--capture` và `--output` cho `camera_inspector.py`; (7) Tích hợp CLI smoke test tự động `python backend/hardware_adapter.py --smoke-test [--mode simulator|fake|physical]`. |
| (21/9)   | Làm rõ phạm vi (TV3) | Minh bạch tiến độ thực nghiệm: (a) Đã hoàn tất 3 lượt kiểm thử nội bộ TV3 phần mềm (Lượt 1: Simulator, Lượt 2: Pytest Suite, Lượt 3: Fake Driver ngắt an toàn); lượt 4 đang chờ TV2 cung cấp SVG nét vẽ tranh; lượt 5 trên giấy thật đang chờ setup phần cứng vật lý; (b) Bỏ tích `[ ]` mục smoke test trên giấy thật trong backlog TV3 (docs/03) do đang chờ máy vẽ và cáp vật lý (Blocked by hardware); (c) Ghi nhận rõ EBB firmware hiện hành chưa có cảm biến kẹt giấy/hết mực; (d) Gửi đề xuất chuẩn hóa endpoint `POST /api/print/pause` và cờ `pause_supported: false` cho TV4. |

| (18/9)   | Làm rõ phạm vi | Rà soát và đồng bộ toàn bộ 4 tài liệu quản lý dự án: (1) Khóa ranh giới ownership giữa TV1 (corpus/fixtures/protocol) và TV4 (experiment runner/logging); (2) Khẳng định Writer Profile thuộc nhóm nghiên cứu mở rộng P2 (Sprint 1–2 chỉ chuẩn bị schema, protocol và fixture độc lập, chưa tích hợp engine, không phải MVP hoàn thành); (3) Phân định rõ ràng thời gian mô phỏng `estimated_draw_time_sec` với thời gian thi công thực tế trên máy vẽ `actual_draw_time_sec`, ghi nhận việc backend gán thời gian mô phỏng vào `actual_draw_time_sec` là implementation/contract debt; (4) Tách biệt rõ 2 bộ baseline đối chứng (Art Mode vs CA-VHC); (5) Cập nhật Giai đoạn 1 ở trạng thái hoàn thành nền tảng phần mềm trong khi kiểm chứng phần cứng AxiDraw vẫn đang bị block. |


| (17/9)   | Quyết định     | Khóa ba trụ cột nghiên cứu cốt lõi của đề tài: (1) Bố trí dấu tiếng Việt theo quy tắc chính tả, cấu trúc âm tiết và ràng buộc hình học (Vietnamese orthography-aware diacritic composition; không tuyên bố engine hiện đã hiểu ngữ nghĩa hoặc ngữ âm, đây là định hướng nghiên cứu khi dấu chưa tham gia đầy đủ vào DAG); (2) Tối ưu hóa đồ thị nét dựa trên Trellis DAG Viterbi mở rộng (Context-aware stroke graph optimization); (3) Cá nhân hóa nét chữ có thể thực thi trên máy vẽ (Plotter-executable personalization) là hướng mở rộng dài hạn. Xác nhận rõ ràng trong toàn bộ tài liệu dự án: mô hình Writer Profile, phân trang đa trang (multi-page) và telemetry đồng bộ thời gian thực từ phần cứng đều là các định hướng nghiên cứu tương lai, chưa triển khai trong mã nguồn hiện hành. |
| (14/9)   | Quyết định     | Đột phá định hướng đề tài học thuật: Nâng cấp bộ sinh chữ viết tay từ "preset font tĩnh" sang "Context-Aware Vietnamese Handwriting Composition (CA-VHC)" kết hợp "Stroke Graph Optimizer" (Viterbi trên Trellis DAG). TV4 (Project Lead & Handwriting Lead) đề xuất kiến trúc CA-VHC phân tích ngữ cảnh (thể loại thư, vị trí từ/dòng, cấu trúc âm tiết và dấu tiếng Việt) để chọn biến thể allographs nét đơn tối ưu, phối hợp với TV2 (Stroke Optimization Lead) mô hình hóa chi phí chuyển động ngòi bút trên Trellis DAG. |
| (14/9)   | Hoàn thành mốc | Tái cấu trúc cơ học toàn diện mã nguồn font pack ra khỏi engine lõi (TV4): tạo package `backend/handwriting/font_packs/` (`__init__.py`, `geometry.py`, `legacy.py`, `omni_casual.py`, `letter_variants.py`), giảm hơn 1.000 dòng code khỏi `handwriting_engine.py`, tách biệt rạch ròi giữa tầng dữ liệu hình học và tầng giải thuật render/auditor mà vẫn bảo toàn tính nhất quán fingerprint và hành vi hệ thống. |
| (14/9)   | Hoàn thành mốc | Hoàn thiện bộ font nét đơn Omni Casual v1 (TV4) với đầy đủ 81 glyphs custom (26 chữ hoa, 26 chữ thường, 10 chữ số, 19 dấu câu thư tay tiếng Việt), audit tự động 72 biến thể nguyên âm tiếng Việt viết thường đạt 0 finding theo bộ kiểm tra polyline hiện tại với min_clearance=0.12 (trong phạm vi bộ kiểm tra hình học hiện được cài đặt). |
| (13/9)   | Hoàn thành mốc | Hoàn thiện phiên bản nền tảng v1 của Module Viết Thư Tay Nét Đơn (TV4): Single-Stroke Vietnamese Handwriting Engine (`backend/handwriting/`), hỗ trợ bảng chữ cái và hệ dấu thanh tiếng Việt theo chuẩn Unicode NFD trong tập ký tự đã định nghĩa, 4 phong cách nét viết (Học sinh, Thảo nghiêng, Thư pháp, Ký tên) và tích hợp API Gateway `/api/ai/generate` (đây là bản nền tảng v1, chưa bao gồm đầy đủ diacritic-aware DAG, delayed-stroke ordering, Writer Profile, multi-page và kiểm chứng trên máy vẽ vật lý). |
| (13/9)   | Quyết định     | Nghiên cứu và phát triển bộ Single-Stroke Vector Centerline Engine (TV4) sử dụng Cubic Bézier kết hợp Catmull–Rom smoothing, duy trì tính liên tục tiếp tuyến C1 tại các nút nội bộ thay vì dùng font TTF viền đôi (Outline font) để máy vẽ thi công đúng 1 nét đơn centerline chuẩn xác, tránh nét mực kép gây rách giấy hoặc đậm bất thường. |
| (13/9)   | Hoàn thành mốc | Chuẩn hóa hình học các glyph tiếng Việt đặc thù (r, s, x, k, h, e, t, i) và hệ thống dấu thanh điệu (hỏi, huyền, sắc, ngã, nặng) chuẩn Bộ GD&ĐT (QĐ 31/2002/QĐ-BGD&ĐT) do TV4 thực hiện trong tập glyphs ban đầu; áp dụng quy tắc tách tầng và điều chỉnh vị trí dấu, kết hợp auditor hình học để phát hiện va chạm trong phạm vi bộ kiểm tra hiện tại và nối nét có chọn lọc (selective ligatures). |
| (13/9)   | Hoàn thành mốc | Nâng cấp giao diện CreateScreen & PreviewScreen (TV4) hỗ trợ chế độ Viết Thư Tay (Handwriting Mode), bổ sung UI controls cho phép tùy biến font, căn lề trang, giãn dòng, nhập text trực tiếp hoặc tải file văn bản (.docx, .txt) (lưu ý: các UI controls đã tồn tại nhưng backend layout engine áp dụng lề thực tế, font size động và phân trang đa trang chưa hoàn thiện). |
| (30/8)   | Hoàn thành mốc | Tích hợp thành công thuật toán tối ưu nét vẽ tranh (TV2) vào Backend (TV4). Nối toàn bộ pipeline: AI Sinh ảnh → Tạo SVG → Caching Metrics → Ghi Log CSV. |
| (30/8)   | Hoàn thành mốc | Nối thành công API sinh ảnh (TV1) vào API Gateway (TV4). Sửa triệt để lỗi path ghi CSV, CORS, và đồng bộ mã lỗi JSON trả về đúng API Spec.  |
| (30/8)   | Quyết định     | Chốt sử dụng Google Gemini API (developer tier) thay thế OpenAI để tối ưu chi phí, cả nhóm dùng chung 1 key kiểm thử. |
| *(24/8)* | Quyết định     | Chốt định hướng đề tài: AI là trọng tâm, phối hợp toàn diện 4 mảng (xem file định hướng gốc)                                                  |
| *(24/8)* | Quyết định     | Chốt chuẩn giao tiếp dữ liệu giữa các mảng: ảnh base64/JSON, SVG cho lệnh vẽ, REST polling cho trạng thái (chi tiết ở `OmniDraw_API_Spec-4.md`) |
| *(24/8)* | Quyết định     | Chốt phân công 4 mảng chính: TV1 (AI Data & Writer Profile Lead), TV2 (Stroke Optimization & Path Planning Lead), TV3 (Hardware, Calibration & Physical Validation Lead), TV4 (Project Lead & Handwriting / CA-VHC Composition Lead). |
| (26/8)   | Hoàn thành mốc | Đã xây dựng được khung UI/UX (sử dụng mock data) do TV4 thực hiện.                                                                           |


**Loại log gợi ý:** `Quyết định` / `Hoàn thành mốc` / `Thay đổi hướng đi` / `Sự cố & cách xử lý`
